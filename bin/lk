# -*- sh -*-
# use> lk | eval # or ln -sv ~/bin/lk ~/ # for> . lk
echo '# lk: load-ssh-keys:'
echo export SSH_AUTH_SOCK=`find /tmp/*ssh* -name "agent*" 2>/dev/null`
echo export SSH_AGENT_PID=`pgrep ssh-agent`

export SSH_AUTH_SOCK=`find /tmp/*ssh* -name "agent*" 2>/dev/null`
export SSH_AGENT_PID=`pgrep ssh-agent`
