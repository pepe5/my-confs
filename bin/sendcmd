#!/usr/bin/python
import pexpect, sys
p = pexpect.spawn ('dtach -A /tmp/1.dtach -z bash')
p.sendline ('')
l = ' '.join (sys.argv [1:])
p.sendline ('echo ' + l) #>? "% #" 
p.sendline (l)
p.sendline ('')
